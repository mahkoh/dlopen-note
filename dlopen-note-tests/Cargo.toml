[package]
name = "dlopen-note-tests"
version = "0.0.0"
edition = "2024"
publish = false

[dev-dependencies]
dlopen-note = { path = "../dlopen-note" }
trybuild = "1.0"
object = { version = "0.36", default-features = false, features = ["read_core", "elf", "std"] }
